@using System.Security.Claims
@{
    if (Context.User.FindFirstValue(ClaimsIdentity.DefaultRoleClaimType) != null)
    {
        switch (Context.User.FindFirstValue(ClaimsIdentity.DefaultRoleClaimType))
        {
            case "GUEST":
                Layout = "NotConfirmUserLayout";
                break;
            case "USER":
                Layout = "_Layout";
                break;
            case "ADMIN":
                Layout = "AdminLayout";
                break;
        }
    }
    else
    {
        Layout = "NotConfirmUserLayout";
    }
}